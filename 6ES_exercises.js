function ex1(num, boolean){
    const lines = "=".repeat(num);
    if (boolean){
        console.log(lines+">");
    }else console.log("<"+lines);
}

function ex2(){
    const arr = []
    let num = prompt(`please enter a valid number`);
    arr.push(num);
    while (num != 0) {
        let num = prompt(`please enter a valid number`);
        /* עוד לא סיימתי */
    }

}